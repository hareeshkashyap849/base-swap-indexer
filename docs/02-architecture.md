# 架构设计 — base-swap-indexer

> **S2 产出物。关卡 G2：CROPS 五项全部有书面答案，且不可逆决策已定。**
> 规范来源：`../../工程蓝图.md` 第四节。

---

## 0. 事实基础（实测，详见 `需求规范.md` §0.2）

```
池      : 0xd0b53D9277642d899DF5C87A3966A349A798F224  (Uniswap V3, fee=500 = 0.05%)
token0  : WETH  0x4200...0006   18 位小数
token1  : USDC  0x8335...2913    6 位小数
事件密度: 936 Swap / 500 块
实测约束: 分段密度差 175×（55 条 vs 9,616 条）；单响应 >10MB 报错；
          6 并发 eth_call 触发限流；eth_getLogs 50000 块范围被拒
```

---

## 1. 架构总览

### 1.1 组件图

```
                    ┌──── 外部（信任边界外）────┐
                    │  Base mainnet RPC（公共）  │  ← 唯一的真实外部依赖
                    │  · getLogs / getBlock      │     免费、限流、无 SLA
                    └───────────┬───────────────┘
                                │ ① 拉日志 + 块头
                                ▼
   ┌────────────────────────────────────────────────┐
   │  Indexer（Node + TypeScript + viem）            │
   │  · 自适应分块  · RPC 轮换  · 断点续跑           │
   │  · reorg 检测与回退                             │
   └───────────┬────────────────────────────────────┘
               │ ② 解码（viem decodeEventLog）+ 换算价格
               ▼
   ┌────────────────────────────────────────────────┐
   │  SQLite（better-sqlite3）                       │
   │  swaps / blocks / indexer_state                 │
   │  索引: (block_number, log_index)、block_number  │
   └───────────┬────────────────────────────────────┘
               │ ③ 聚合查询
               ▼
   ┌────────────────────────────────────────────────┐
   │  API（Fastify + TypeScript）                    │
   │  /api/swaps  /api/ohlcv  /api/stats  /api/health│
   └───────────┬────────────────────────────────────┘
               │ ④ HTTP JSON
               ▼
   ┌────────────────────────────────────────────────┐
   │  前端看板（React + Vite + Recharts）            │
   │  概览数字 · 价格/成交量图 · 成交明细            │
   └────────────────────────────────────────────────┘
```

**信任边界只有一条**：Indexer → RPC。
跨过这条线意味着"我们相信 RPC 返回的数据是真的"。RPC 由第三方运营，可能返回陈旧或不正确数据。
**缓解**：价格同时由 `sqrtPriceX96` 和 `tick` 两条独立路径推导并交叉校验（见 §6 INV-3）。

### 1.2 组件清单

| 组件 | 类型 | 职责 | 运行在哪 | 谁能改 |
|---|---|---|---|---|
| `Indexer` | 本地进程（CLI） | 拉日志、解码、写库、续跑、reorg 处理 | 使用者本机 | 开源，任何人 |
| `SQLite 文件` | 数据存储 | 存 Swap、块头、索引游标 | 本机文件 | 使用者 |
| `API` | 本地 HTTP 服务 | 查询与聚合 | 使用者本机 | 开源 |
| `前端` | 静态站点（Vite build） | 可视化 | 使用者浏览器 | 开源 |
| **Base RPC** | **外部依赖** | 提供链上数据 | 第三方 | **不可控** |

---

## 2. CROPS 关卡

> ⚠️ **本项目是只读索引器，不托管资产、不发交易、无用户账户。**
> 因此 CROPS 里"谁能动用户资金"这类问题**结构性不适用**。
> **我会明确标注"不适用"，不编造答案**——把不适用的项写成"我们很安全"是误导。

### C — Custody（谁能动资金？）

| 问题 | 答案 |
|---|---|
| 用户资金存在哪？ | **不适用**。本项目不持有任何资产，无存款/取款/托管 |
| 谁能单方面转移资金？ | **不适用**。代码里没有签名、没有私钥、没有任何 `eth_sendRawTransaction` 调用 |
| 升级权限在谁手里？ | **不适用**（无链上合约） |
| 有没有紧急提款函数？ | **不适用** |
| 管理员能改用户余额吗？ | **不适用** |

**结论**：**零托管**。
**最重要的安全性质**：**本项目在设计上无法造成资金损失**——它只调用只读 RPC 方法（`eth_getLogs` / `eth_getBlockByNumber` / `eth_call`），**代码库里不存在私钥、助记词、签名逻辑**。
> 这条对作品集是优点：评审者可以放心 clone 运行，不存在"跑一下就被盗"的风险。

### P — Privacy（暴露什么？）

| 问题 | 答案 |
|---|---|
| 哪些数据被处理？ | Base 主网**公开**的 Swap 事件 + 公开块头 |
| 是否收集个人身份信息？ | **否**。链上地址是假名数据，本项目不抓取 IP、不做用户画像、无登录 |
| 是否可关联行为？ | 链上数据本身公开可关联——但**这是链的性质，不是本项目的收集行为** |
| 是否有链下数据上链？ | 否（只读，不上链） |
| 数据库里存了敏感信息吗？ | 否。只有公开链上事件 |
| 是否会把数据发给第三方？ | **否**。前端只调本地 API，无遥测、无分析脚本、无外链追踪 |

**结论**：**中性**。处理的是公开数据，不产生新的隐私风险。
> **工程注意**：README 里要写明"本索引器只索引公开链上数据"，避免使用者误以为涉及隐私数据。

### E — Exit（用户怎么退出？）

| 问题 | 答案 |
|---|---|
| 使用者如何停止使用？ | `Ctrl+C` 停进程；删 `data/` 目录即彻底清除 |
| 有数据锁定/厂商绑定吗？ | **无**。SQLite 是标准文件格式，`sqlite3` 命令行可直接读；schema 写在仓库里 |
| 退出成本？ | 零 |
| 会不会有"停不掉"的状态？ | 否。无后台守护、无系统服务注册、无开机自启 |
| 数据可迁移吗？ | 可。schema + SQL 全在仓库；`README` 给 Postgres 迁移路径 |

**结论**：**退出门极宽**。单文件数据库 + 无守护进程 + 标准格式，使用者随时可弃用并把数据带走。

### C — Censorship-resistance（谁能阻止运行？）

| 问题 | 答案 |
|---|---|
| 谁能阻止使用者运行？ | **RPC 提供方**——公共端点可限流、可封 IP、可停服。**这是最现实的失效点**（已实测到限流） |
| 排序器是否中心化？ | **不适用**（不提交交易，与排序器无关） |
| 能否冻结特定地址？ | 不适用（不涉及账户） |
| 被索引的数据源会消失吗？ | 不会。Base 主网数据是永久公开的；换一个 RPC 即可继续 |
| 前端下线还能用吗？ | **能**。API 与索引器独立于前端；也可直接用 `sqlite3` 查库或 `curl` 打 API |

**结论**：唯一的审查点是 RPC，**缓解措施是 RPC 多端点轮换（F7 已列为必须）**。
数据本身在公链上，**本项目不存在"数据被单方扣留"的风险**。

### C — Centralization（单点故障清单）

| # | 单点 | 失效后果 | 缓解措施 | 是否接受 |
|---|---|---|---|---|
| 1 | **公共 RPC 端点** | 索引停滞；限流导致抓取变慢 | **多端点轮换 + 指数退避**（F7/F3） | [x] 接受 |
| 2 | RPC 提供方返回**错误/陈旧数据** | 价格与分析结果错误 | `sqrtPriceX96` 与 `tick` 双路径交叉校验（INV-3）；存 `blockHash` 便于核对 | [x] 接受 |
| 3 | 本机磁盘/进程 | 索引中断 | 断点续跑（F2）；SQLite 事务保证不半写 | [x] 接受 |
| 4 | 单一数据源（只有这一个池） | 池子若迁移/停用则无数据 | 池地址写在配置里，可换；属 N1 范围外 | [x] 接受 |
| 5 | `better-sqlite3` 是原生模块 | 需要编译工具链，特定 Node 版本可能装不上 | 固定 Node 版本要求；README 说明；必要时可换 `node:sqlite` | [x] 接受 |

**结论**：**两个真实单点都在外部**（RPC 可用性、RPC 数据可信度），且都有明确的缓解措施写进功能规格。
**内部没有单点**——无服务器、无数据库服务、无消息队列、无容器编排。

---

## 3. 不可逆决策（冻结）

| 决策 | 选定 | 被否方案 | 否掉的理由 | 冻结日期 |
|---|---|---|---|---|
| **索引目标** | Uniswap V3 WETH/USDC 0.05% 池（Base） | ① USDC Transfer ② Morpho ③ Aerodrome | ① 太通用、无分析语义（只有转账）②③ **实测被 RPC 拒绝**（`getLogs` 范围限制）；且 Morpho 事件过稀（50000 块内无数据）。选中的池有 936 Swap/500 块、7 个含价格信息的字段 | 2026-09-15 |
| **语言** | **TypeScript** | JavaScript | 目标是 TypeScript 岗位；类型是索引器正确性的第一道防线（解码后的字段类型必须可靠） | 2026-09-15 |
| **链上交互库** | **viem** | ethers.js | viem 的类型推导与 `parseAbi` 更严格，且是当前主流新项目选择。**已实测可用**（2.56.5，成功拉取真实日志） | 2026-09-15 |
| **数据库** | **SQLite（better-sqlite3）** | ① Postgres ② DuckDB ③ 先内存后落盘 | ① 要求评审者装 Docker/服务 → **给作品集设门槛**（S0 约束 2）② DuckDB 偏分析、写并发弱 ③ 内存无法续跑 | 2026-09-15 |
| **不用 ORM** | 手写参数化 SQL | Prisma / Drizzle | ① 索引器的核心性能点是查询与索引设计，ORM 会遮蔽它 ② 少一层依赖，clone 后装包更快 ③ 手写 SQL 本身就是 JD 要求的能力 | 2026-09-15 |
| **数据模型** | 存**解码后的结构化列** | 存原始 `log` JSON 再解析 | ① 列式存储可用 SQL 直接聚合，API 无需二次解析 ② 索引可建在真实列上 ③ **代价**：schema 变更需迁移——本项目事件结构固定，可接受 | 2026-09-15 |
| **价格存储** | 存**原始字段** + **查询时换算** | 入库时就算好价格 | ① 换算公式若发现错误，原始数据仍在，可重算 ② 避免"错误被固化进数据库" | 2026-09-15 |
| **实时机制** | **轮询** | WebSocket | 看板刷新频率低（秒级足够）；WS 增加连接管理与重连复杂度，收益不明显 | 2026-09-15 |
| **API 框架** | **Fastify** | Express / Hono | ① 内置 schema 校验（正好满足 F12"非法输入返回 4xx 不得 500"）② 性能优于 Express ③ 技能库有覆盖（fastify 10 文件） | 2026-09-15 |
| **前端** | **React + Vite** | Next.js | ① 本项目前端是**纯客户端看板**，无 SSR/SEO 需求 ② Vite 更轻、启动更快 ③ Next.js 的价值（路由/SSR）在此用不上——违反反过度设计 | 2026-09-15 |
| **测试** | **`node --test`（内置）** | Jest / Vitest | ① Node 24 内置测试运行器已够用，**少一个依赖** ② 评审者 `npm test` 即可，无需配置 | 2026-09-15 |
| **reorg 处理策略** | 存 `blockHash`，发现不一致则**回退重索引** | ① 忽略 reorg ② 只索引"安全确认数"之前的块 | ①**忽略是错的**——数据会永久错误 ② 保守等待会显著增加滞后。回退是正确且可实现的折中 | 2026-09-15 |

### 3.1 被否方案里值得单独说明的一项

**为什么不用 subgraph / Ponder 这类现成索引框架？**

| 方案 | 否决理由 |
|---|---|
| The Graph 托管 subgraph | ① 需要部署到托管服务，引入外部账号与配额 ② **把"索引"这个核心技能外包给了别人**——而索引正是本项目要证明的能力 |
| Ponder | 是很好的框架，但 ① 它把分块、reorg、续跑都封装好了，**评审者看不到我怎么解决这些问题** ② 技能库有 `ponder` 技能（27 文件），**会作为对比方案在 README 里提及**，说明我知道它存在以及为何此处手写 |

> **这是一个刻意的取舍**：手写索引器**更慢、更容易出 bug**，但它**把工程能力暴露出来**。
> README 会明确写这条取舍理由——**"我知道有现成框架，我选择手写，因为我要展示的是这段逻辑本身"**。

---

## 4. 采用的标准与接口

| 项 | 内容 | 状态 | 主源核对日期 |
|---|---|---|---|
| Uniswap V3 `Swap` 事件签名 | `Swap(address,address,int256,int256,uint160,uint128,int24)` | 链上实测（936 条成功解码） | 2026-09-15 |
| Uniswap V3 `slot0` | `(uint160 sqrtPriceX96, int24 tick, ...)` | 链上实测 | 2026-09-15 |
| ERC-20 `decimals`/`symbol` | 用于价格换算 | 链上实测（WETH=18, USDC=6） | 2026-09-15 |
| REST / JSON | API 契约 | — | — |
| **本项目不涉及** | ERC-4626 / EIP-1967 / EIP-712 / 任何链上合约标准 | — | — |

> 本项目**不部署合约**，因此不涉及合约标准。相关能力见 `../base-yield-vault/`（ERC-4626 项目）。

---

## 5. 价格换算（**核心数学，必须写清楚**）

### 5.1 从 `sqrtPriceX96` 推导

Uniswap V3 的定义：

```
sqrtPriceX96 = sqrt(price) × 2^96
其中 price = token1 的数量 / token0 的数量      （都用最小单位）
```

因此：

```
rawPrice        = (sqrtPriceX96 / 2^96)²          // token1 最小单位 / token0 最小单位
priceAdjusted   = rawPrice × 10^(decimals0 - decimals1)   // 调整为人类单位比
```

**本项目代入实际值**（`decimals0 = 18`(WETH)，`decimals1 = 6`(USDC)）：

```
priceAdjusted = rawPrice × 10^(18 - 6) = rawPrice × 10^12
              = token1 数量 / token0 数量 = USDC / WETH
              = **WETH 的 USDC 价格** ✅
```

实测验证：`rawPrice = 2.44349691e-9` → `× 10^12` = `2443.50` → **WETH ≈ $2443.5**。

> ⚠️ **方向陷阱（我在核实前踩过）**：若误以为 token0 是 USDC，就会取倒数得到 `0.000409`，整个价格分析全错。
> 因此：
> 1. `token0`/`token1` **必须从链上读取**，绝不硬编码假设；
> 2. 价格方向由 **INV-3 交叉校验** 兜底（见 §6）。

### 5.2 成交量换算

`Swap` 事件的 `amount0`/`amount1` 是 **int256 且带符号**：

| 字段 | 符号 | 含义 |
|---|---|---|
| `amount0 > 0` | 池子**收到** WETH | 用户卖出 WETH（卖） |
| `amount0 < 0` | 池子**付出** WETH | 用户买入 WETH（买） |
| `amount1` | 与 `amount0` **符号相反**（恒定乘积） | 对应 USDC 流向 |

**成交量以 USDC 计**：`volumeUsdc = |amount1| / 10^6`。

**方向判定**：`amount0 > 0` → 卖出 WETH；`amount0 < 0` → 买入 WETH。

### 5.3 从 `tick` 推导（独立路径，用于交叉校验）

```
priceFromTick = 1.0001^tick × 10^(decimals0 - decimals1)
```

实测：`tick = -198309` → `1.0001^-198309 × 10^12 = 2443.32`，
与 `sqrtPriceX96` 路径的 `2443.50` 一致（差异来自 tick 是价格的离散对数近似）。

**两者一致性作为 INV-3 断言**：`|ln(rawPrice / 1.0001^tick)| < 0.001`。

---

## 6. 不变量（**测试的依据**）

> 蓝图纪律 3："测试通过"不是证据，"验证了哪些不变量"才是。

### INV-1 幂等性
> **对同一区块范围重复索引，`swaps` 表行数与内容不变。**

测试：跑两次索引，断言 `COUNT(*)` 与关键字段哈希相同。
**为什么重要**：续跑/重试是索引器的常态；不幂等就会产生重复成交，所有聚合指标翻倍。

### INV-2 分块完整性
> **自适应分块不得漏块、不得重复块。**

测试：用真人造的"某段失败"场景，断言最终覆盖的区块集合恰好等于目标区间。
**为什么重要**：分块是索引器最容易出错的地方——**漏块是静默错误**（数据看起来正常，实际缺一段）。

### INV-3 价格双路径一致
> **每个 Swap 的 `sqrtPriceX96` 推导价格与 `tick` 推导价格一致（对数差 < 0.001）。**

测试：对全部入库 Swap 断言；不一致的条数必须为 0。
**为什么重要**：① 兜住 token 顺序猜错（方向陷阱）② 兜住 RPC 返回错数据 ③ 是"我理解这个协议"的可执行证据。

### INV-4 reorg 后数据自洽
> **检测到 reorg 并回退后，`swaps` 表中不存在来自已废弃区块的记录。**

测试：注入一个 `blockHash` 不一致的场景，断言回退区块及其之后的记录被清除且重新索引。
**为什么重要**：这是索引器**最常被忽视**的正确性问题；能处理它是资深信号。

### INV-5 聚合与明细一致
> **`/api/stats` 的 `trades` 等于该时间窗内 `swaps` 表的行数；`volumeUsdc` 等于逐条 `|amount1|` 之和。**

测试：用独立 SQL 重算，与 API 返回值比对。
**为什么重要**：兜住聚合 SQL 的 `WHERE`/`GROUP BY` 写错——这类 bug 会让看板数字与明细对不上。

### INV-6 符号约定正确
> **每条 Swap 满足 `sign(amount0) = -sign(amount1)`。**

测试：断言全部行满足。
**为什么重要**：直接验证 §5.2 的方向判定逻辑；写反了会让"买/卖"标签全反。

---

## 7. 失败模式分析

| # | 失败场景 | 触发条件 | 系统行为 | 影响 | 预案 |
|---|---|---|---|---|---|
| 1 | RPC 限流 | 并发或高频调用 | 切换下一端点 + 指数退避 | 索引变慢，不中断 | F7 已实现；**实测触发过** |
| 2 | 单次响应过大 | 块区间日志过多（>10MB） | 捕获错误 → 块高**减半**重试 | 该段变慢，不丢数据 | F4 自适应分块；**实测触发过** |
| 3 | `getLogs` 范围被拒 | 区间过大 | 同上（缩小区间）；连续失败则明确报错 | 不静默跳过 | F3 要求"明确报错" |
| 4 | 进程被中断 | Ctrl+C / 崩溃 | 已提交事务保留；重跑从 `lastIndexedBlock` 续 | 无损失 | F2 续跑；SQLite 事务 |
| 5 | 链重组（reorg） | Base 出块快，短 reorg 常见 | 检测 `blockHash` 不一致 → 回退重索引 + 警告 | 短暂数据回滚 | F6 + INV-4 |
| 6 | RPC 返回错误数据 | 恶意/故障端点 | INV-3 交叉校验失败 → 报警 | 可被检出 | INV-3 |
| 7 | 数据库写一半失败 | 磁盘满 / 进程被杀 | SQLite 事务回滚，不留半写状态 | 无 | 所有写入用事务 |
| 8 | `better-sqlite3` 装不上 | 缺编译工具链 | `npm install` 失败 | 阻塞评审者 | 说明 Node 版本要求；备用 `node:sqlite` |
| 9 | 池子地址变更/停用 | 协议升级 | 无数据 | 项目失效 | 地址配置化；属 N1 范围 |
| 10 | 前端 API 未启动 | 使用者只跑了前端 | 看板显示明确错误提示 | 可诊断 | 错误态 UI |

---

## 8. 反过度设计检查

> `ethskills/ship`：**1-2 个合约能做的事不要写 5 个**（此处类推到服务与模块）。

- [x] **服务数量**：**3 个**（索引器 / API / 前端），理由：这三个职责的运行周期与失败模式完全不同——索引器是**批处理+长驻**，API 是**请求响应**，前端是**静态资源**。合并会牺牲可测试性。
- [x] **有没有"为了将来可能用到"的抽象层？** 主动去掉了：
      - ❌ 去掉 `IChainAdapter` 多链抽象 → 只索引一条链（N2）
      - ❌ 去掉 `IProtocolAdapter` 多协议抽象 → 只索引一个池（N1）
      - ❌ 去掉 ORM 抽象 → 手写 SQL（§3）
      - ❌ 去掉 Redis / 缓存层 → SQLite 直接查，1.8 万行无需缓存
      - ❌ 去掉 WebSocket 层 → 轮询足够（§3）
      - ❌ 去掉 Docker / compose → 刻意降低运行门槛（N5）
- [x] **有没有可复用现成实现而不自研的？** 有，**已全部复用**：
      - `viem` 负责 RPC、ABI 解码、大整数与 decimals 换算（**不自研**）
      - `better-sqlite3` 负责存储与事务（**不自研**）
      - `fastify` 负责 HTTP 与 schema 校验（**不自研**）
      - `react` / `recharts` 负责渲染（**不自研**）
      - **唯一自研的核心逻辑**：自适应分块、reorg 回退、价格换算、聚合 SQL ——**这四项正是本项目要展示的能力**
- [x] **有没有引入不必要的外部依赖？** 依赖清单刻意压到最小：运行时 4 个（viem / better-sqlite3 / fastify / react 系），开发期 3 个（typescript / tsx / vite）。**不用 ORM、不用测试框架、不用 Docker。**

**结论**：**复杂度匹配一个作品集项目的需求，且每一处省略都有书面理由。**
反向说：**如果这个项目写了 15 个模块、加了多链抽象和容器编排，那才是问题**——它说明设计者分不清"展示能力"和"堆砌技术"。

---

## 9. 关卡 G2 确认

- [x] CROPS 五项全部有书面答案（**含 5 处诚实的"不适用"标注**，未编造）
- [x] 不可逆决策已定，且记录了被否方案与理由（12 项）
- [x] 采用的标准已对主源核实（Swap 事件与 slot0 均为链上实测）
- [x] 失败模式分析已覆盖主要场景（10 项，其中 3 项**已实测触发过**）
- [x] 反过度设计检查通过（3 服务 / 7 依赖 / 4 项自研核心逻辑）
- [x] **价格换算数学已写清并实测验证**（§5，含方向陷阱说明与 INV-3 兜底）
- [ ] **执行者已知晓**：无托管（不涉及资金）、无部署（纯本地）、两个外部单点（RPC 可用性与可信度）

**冻结日期**：______　**确认人**：______

> **待执行者确认**：`需求规范.md` Q4（是否留真实姓名）/ Q5（仓库是否 public）/ Q6（是否补 GitHub profile）。
> 这三项不影响架构，可与 S3 并行。
>
> 下一步：S3 脚手架 → S4 实现（`../../工程蓝图.md` §5）。
