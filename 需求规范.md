# 需求规范 — base-swap-indexer

> **S0 产出物。关卡 G0：本文件确认后方可进入 S2 之后的阶段。**
> 规范来源：`../../工程蓝图.md` 第二节。
>
> **项目定位**：作品集项目。目标岗位 = **Web3 后端 / 区块链集成 / Web3 全栈**（TypeScript / Node.js / API / 数据库 / 钱包集成 / **链上事件索引** / Solidity）。
> **不是**安全审计岗、不是量化研究岗。

---

## 0. 需求原始陈述（照抄）

```
我需要作品集来充实简历，目标是胜任全球远程的 Web3 后端 / 区块链集成 / 全栈 dApp 岗位。
重点关注 TypeScript/Node.js、API、数据库、钱包集成、链上事件索引、Solidity 和 Foundry。
优先考虑支持中国大陆或亚洲时区的远程岗位。
不做：安全审计、形式化验证、底层协议、资深架构（除非实际经历能支撑）。
红线：不编造主网部署、审计、资金管理成果；不把 AI skill/工具安装记录当工作经验。
GitHub 用户名 hareeshkashyap849；仓库名 base-swap-indexer。
索引范围：单池 · 最近 10k 块。前端重点：数据看板。
```

**需求来源**：执行者本人（2026-09-15 会话）
**最终决策人**：执行者本人

### 0.1 从原始陈述推导出的隐含约束

1. **受众是工程师，不是 HR**。技术面试官会看代码、会追问。设计要给追问留出深度。
2. **必须能"一条命令跑起来"**。评审者不会为作品集装 Docker → **SQLite 为默认**，Postgres 只留迁移路径。
3. **索引器是本项目的核心技能靶心**。JD 原文出现过"利用 Subgraphs 等框架创建自定义区块链索引器"——**索引器是能单独写进 JD 的硬技能**。
4. **不能声称生产/资金成果**。本项目的正确表述是"个人项目，索引真实主网数据，无资金托管"。

### 0.2 已核实的事实基础（**全部实测，非文档推断或记忆**）

| 事实 | 值 | 核实方式 |
|---|---|---|
| 目标池 | `0xd0b53D9277642d899DF5C87A3966A349A798F224` | RPC `eth_call` |
| 协议 | Uniswap V3（fee=500 即 0.05%） | `fee()` 返回 500 |
| **token0** | **WETH** `0x4200...0006`，**18 位小数** | `token0()` + `decimals()` |
| **token1** | **USDC** `0x8335...2913`，**6 位小数** | `token1()` + `decimals()` |
| 当前 `sqrtPriceX96` | `3916385922366450481262234` | `slot0()` |
| 当前 `tick` | `-198309` | `slot0()` |
| 当前 `liquidity` | `1505864926519222278` | `liquidity()` |
| 价格自洽性 | `sqrtPriceX96` 与 `tick` **一致**（误差 < 0.001） | 两侧独立反算比对 |
| 事件密度 | **936 条 Swap / 500 块**（Base mainnet 区块 51,345,386 附近） | `getLogs` 实测 |
| 日志密度不均 | 分段实测：某段 55 条，邻段 **9,616 条** | 实测 |
| 公共 RPC 单响应上限 | 超 ~10MB 时 viem 抛 `ResponseBodyTooLargeError` | 实测（2000 块直接拉失败） |
| 公共 RPC 限流 | 6 个并发 `eth_call` → `over rate limit` | 实测 |
| `eth_getLogs` 范围限制 | 50000 块范围被 RPC 拒绝 | 实测（Morpho/Aerodrome 查询失败） |

> ⚠️ **本项目的价格推导方向依赖于"token0=WETH"这一事实。**
> 我在核实前曾假设 token0 是 USDC，**那会导致价格算成倒数**。
> 这正是蓝图纪律 1 要防的错误，已写入测试断言（见 S2 §6）。

---

## 1. 功能规格

### 1.1 索引器（核心）

| # | 用户故事 | 验收条件（可测试） | 优先级 |
|---|---|---|---|
| F1 | 作为使用者，我要能把历史 Swap 事件抓下来，以便有数据可查 | 回填最近 10,000 块，落库 Swap 条数 > 0，且每条含 `blockNumber` / `txHash` / `logIndex` / `sender` / `recipient` / `amount0` / `amount1` / `sqrtPriceX96` / `liquidity` / `tick` | 必须 |
| F2 | 作为使用者，我要索引能从中断处继续，而不是每次重跑 | 杀掉进程后重跑，已索引区块不重复抓取（按 `lastIndexedBlock` 续跑）；重复运行结果条数不变（幂等） | 必须 |
| F3 | 作为使用者，我要索引不会因单段失败而整体失败 | 某一段 `getLogs` 失败时，自动缩小块高重试；连续失败超过阈值才中止并**明确报错**（不得静默跳过） | 必须 |
| F4 | 作为使用者，我要索引能处理日志密度突变 | 分块大小自适应：遇到响应过大或限流时**减半**；成功后**逐步放大**（上限可配） | 必须 |
| F5 | 作为使用者，我要索引能跟上链头 | `--follow` 模式下持续索引新块，推进 `lastIndexedBlock` | 应该 |
| F6 | 作为使用者，我要能检测链重组 | 发现已入库区块的 `blockHash` 与链上不一致时，**回退该区块及之后的记录并重新索引**，并打印警告 | 应该 |
| F7 | 作为使用者，我要能对多个 RPC 容错 | RPC 返回限流/错误时自动切换下一个端点；全部失败才抛错 | 必须 |

### 1.2 API

| # | 用户故事 | 验收条件（可测试） | 优先级 |
|---|---|---|---|
| F8 | 作为前端/集成方，我要能分页取成交 | `GET /api/swaps?limit=&cursor=` 返回按 `blockNumber, logIndex` 倒序的成交；`limit` 上限受服务端限制（不得任意大） | 必须 |
| F9 | 作为集成方，我要能拿到价格序列 | `GET /api/ohlcv?interval=1h&limit=` 返回 OHLC（价格用 USDC 计价），字段为 `ts, open, high, low, close, volumeUsdc, trades` | 必须 |
| F10 | 作为集成方，我要能拿到概览指标 | `GET /api/stats` 返回 `vwap` / `volumeUsdc` / `trades` / `uniqueTraders` / `lastIndexedBlock` / `priceNow` | 必须 |
| F11 | 作为集成方，我要能查到索引健康度 | `GET /api/health` 返回 `ok` / `lastIndexedBlock` / `chainHead` / `lag`（块高差） | 应该 |
| F12 | 作为使用者，我要 API 能处理异常输入 | 非法 `interval` / 非数字 `limit` / 超范围参数 → 返回 4xx 与明确错误信息，**不得 500** | 必须 |

### 1.3 前端看板

| # | 用户故事 | 验收条件 | 优先级 |
|---|---|---|---|
| F13 | 作为访客，我要一眼看到池子现状 | 首屏显示：当前价格、24h 成交量、成交笔数、独立交易者数 | 必须 |
| F14 | 作为访客，我要看价格走势与成交量 | 价格折线/面积图 + 成交量柱状图，可切换时间粒度（如 1h / 1d） | 必须 |
| F15 | 作为访客，我要看最近成交明细 | 表格显示时间、方向（买/卖）、金额（USDC 与 WETH）、价格 | 必须 |
| F16 | 作为访客，我要知道数据是否新鲜 | 页面显示"数据截至区块 X / 更新于 Y"，滞后时给出视觉提示 | 应该 |

### 1.4 交付物

| # | 验收条件 | 优先级 |
|---|---|---|
| F17 | 独立 git 仓库，README 含：项目说明、架构图、如何一条命令跑起来、方法学、已知限制 | 必须 |
| F18 | README 中的每个命令**都实际跑过**（文档即测试） | 必须 |
| F19 | 有测试且 `npm test` 通过（价格换算、分块、reorg、API 校验） | 必须 |

---

## 2. 非功能规格

### 2.1 运行环境与部署

| 项 | 答案 | 备注 |
|---|---|---|
| 目标链 | **Base mainnet**（chainId 8453） | 真实主网数据，只读，无资金风险 |
| 是否写链 | **否** | 只读索引器；无交易、无私钥、无资金 |
| 数据库 | **SQLite（默认）** | 免配置、文件可入仓库、评审者 clone 即跑 |
| Postgres | **仅文档化迁移路径** | 不设为前置条件（见约束 2） |
| Docker | **不需要** | 刻意不引入：会给评审者设门槛 |
| Node 版本 | >= 20（本机 24.9.0 实测） | 用 `node --test` 免额外测试依赖 |

### 2.2 规模

| 项 | 量级 | 影响 |
|---|---|---|
| 索引区块数 | 10,000（约 5.5 小时链上时间，Base 2s 出块） | 按 936/500 块估算约 **1.8 万条 Swap** |
| 数据库体积 | 预估 < 10 MB | SQLite 完全够用 |
| 并发用户 | 1（评审者） | 无需负载均衡 |
| 上游依赖 | 公共 RPC（免费、会限流、无 SLA） | **必须容错**，见 F3/F7 |

### 2.3 代币

- [x] 是否发行代币？ **否**
- 不涉及代币经济 → 不触发 `templates/代币经济设计.md`

### 2.4 合规

| 项 | 答案 |
|---|---|
| 辖区 / 目标市场 | 无（只读公开链上数据，无用户、无资金） |
| 是否涉及证券发行 | 否 |
| 是否涉及支付牌照 | 否 |
| 是否需要 KYC/AML | 否 |
| 是否涉及个人数据 | **否**——链上地址是假名的公开数据，本项目不收集任何个人身份信息 |
| 制裁筛查 | 否 |

> **说明**：只读索引公开链上数据，不托管资产、不面向用户收款，因此不涉及合规。
> 正式客户项目此节必须由客户法务填写——**技能库不保证合规**。

### 2.5 性能与成本

| 项 | 目标 | 怎么测 |
|---|---|---|
| 回填 10k 块耗时 | 记录实测值，不设硬指标 | 索引器自身计时输出 |
| API P95 响应 | < 200 ms（本地、1.8 万行） | 实测记录 |
| 成本 | **$0** | 公共 RPC 免费；无部署费用（纯本地） |
| RPC 调用量 | 记录总调用数（用于说明限流下的工程取舍） | 索引器统计输出 |

---

## 3. 明确不做的事（范围边界）

| # | 不做的事 | 为什么不做 | 已知晓 |
|---|---|---|---|
| N1 | 多池 / 多协议 | 单池已能完整展示索引、解码、聚合、API、前端全链路；多池只增加重复劳动 | [ ] |
| N2 | 多链 | 同上 | [ ] |
| N3 | 写链 / 交易发送 / 钱包连接 | 本项目是**只读索引器**。钱包集成另立项目，不混进来 | [ ] |
| N4 | WebSocket 实时推送 | 用轮询即可满足看板；WS 属增强项 | [ ] |
| N5 | Docker / K8s | 刻意不给评审者设门槛 | [ ] |
| N6 | ORM（Prisma/Drizzle） | 用参数化 SQL。**理由**：手写 SQL 能展示数据库理解，ORM 会遮蔽索引与查询计划问题 | [ ] |
| N7 | 历史全量索引（从创世块） | 10k 块足够展示能力；全量需付费 RPC 与数小时 | [ ] |
| N8 | 认证 / 多用户 / 限流中间件 | 无用户体系 | [ ] |
| N9 | 部署到服务器 / 域名 | 零成本约束；本地可跑即可。**不声称已部署** | [ ] |
| N10 | 价格预言机 / 交易策略 | 不涉及 | [ ] |

---

## 4. 交付形式与验收方式

| 项 | 内容 |
|---|---|
| 交付形式 | 独立 git 仓库（TypeScript 全栈：索引器 + API + 前端） |
| **验收方式** | ① `npm install && npm run index && npm run dev` 一条链路跑通 ② `npm test` 通过 ③ 看板能显示真实价格与成交 |
| 验收人 | 执行者本人，并按**"技术面试官 15 分钟扫代码"**视角自检 |
| 目标交付日 | 第一轮（最小闭环） |
| 变更处理 | 记入 `执行清单.md` §5 |

**第一轮交付物**：
1. `src/indexer/` 索引器（自适应分块、续跑、RPC 轮换）
2. `src/api/` REST API（swaps / ohlcv / stats / health）
3. `web/` 数据看板
4. `test/` 测试（含**真实数据的 fixture**）
5. `README.md`：说明 + 架构 + 一条命令跑起来 + 方法学 + 已知限制

---

## 5. 待确认问题清单

| # | 问题 | 为什么要问 | 答复 | 日期 |
|---|---|---|---|---|
| Q1 | 仓库名 `base-swap-indexer` 确认？ | 已选 | ✅ 2026-09-15 | 2026-09-15 |
| Q2 | 索引范围单池 10k 块确认？ | 已选 | ✅ 2026-09-15 | 2026-09-15 |
| Q3 | 前端重点为数据看板确认？ | 已选 | ✅ 2026-09-15 | 2026-09-15 |
| Q4 | **README 里是否写真实姓名/联系方式** | 隐私 vs 可联系性 | **待确认** | |
| Q5 | **是否公开该仓库（public）** | 私有仓库无法作为作品集；但需确认无敏感信息 | **待确认**（建议 public） | |
| Q6 | GitHub profile 是否先补齐 name/bio/location | 空 profile 点开是负信号 | **待确认** | |

---

## 6. 关卡 G0 确认

- [x] 每条功能规格都有**可测试的**验收条件（F1–F19）
- [x] 非功能规格已填（链、规模、代币、合规、性能）
- [x] "不做的事"已列明（N1–N10）
- [x] 事实基础已对主源核实（§0.2，含一次自我纠正）
- [x] 持续成本已告知 → **$0**（公共 RPC 免费、纯本地运行）
- [ ] **执行者书面确认本规格**（Q4–Q6 待答）

**确认人**：______　**日期**：______　**确认方式**：______

> **未确认不得进入 S2 之后的任何阶段**（蓝图 G0）。
> Q4–Q6 是隐私与可见性决策，不影响架构，可与 S2 并行确认。
